default: &default
  url: <%= ENV.fetch("DATABASE_URL") %>
  pool: <%= ENV.fetch("DB_POOL", 5) %>
  timeout: <%= ENV.fetch("DB_TIMEOUT", 5000) %>

production:
  <<: *default

development:
  <<: *default

test:
  <<: *default
